filebeat.inputs:
  - type: container
    enabled: true
    paths:
      - '/var/lib/docker/containers/*/*.log'
    
    # Parse Docker JSON logs
    json.keys_under_root: true
    json.add_error_key: true
    json.message_key: log
    
    # Processors to enrich data
    processors:
      - add_docker_metadata:
          host: "unix:///var/run/docker.sock"
      
      - decode_json_fields:
          fields: ["message"]
          target: "json"
          overwrite_keys: true
          add_error_key: true
      
      - add_fields:
          target: ''
          fields:
            environment: production
            cluster: wiz-car-rental

      # Drop logs from ELK services to avoid circular logging
      - drop_event:
          when:
            or:
              - contains:
                  container.name: "elk_"
              - contains:
                  container.name: "filebeat"
              - contains:
                  container.name: "elasticsearch"
              - contains:
                  container.name: "kibana"

# CRITICAL FIX: Output directly to Elasticsearch (no Logstash)
# Use service name 'elk_elasticsearch' not 'elasticsearch'
output.elasticsearch:
  hosts: ["elk_elasticsearch:9200"]
  index: "wiz-logs-%{+yyyy.MM.dd}"
  
# Logging
logging.level: info
logging.to_files: true
logging.files:
  path: /usr/share/filebeat/logs
  name: filebeat
  keepfiles: 7
  permissions: 0644

# Filebeat monitoring
monitoring.enabled: false